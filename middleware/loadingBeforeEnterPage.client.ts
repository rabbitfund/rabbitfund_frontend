// import { useGlobalStateStore } from '@/stores/globalState';

// export default defineNuxtRouteMiddleware((to, from) => {
//   const globalState = useGlobalStateStore();
//   if (to.path === '/users/signin') {
//     globalState.recordLastRoute(from.path);
//   }
// });
